<%- include ("../partials/header.ejs") %>
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            padding: 0;
            overflow-y: auto;
            box-sizing: border-box;
            font-family: "Orbitron", sans-serif;
        }

        body {
            background-color: #000000;
            color: #ffffff;
        }

        .main-content {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 20px;
            gap: 10px;
        }

        .profile-container,
        .modify-profile-container {
            width: 100%;
            /* or max-width: 400px; */
        }

        .profile-container,
        .preview-container {
            width: 100%;
            /* or max-width: 400px; */
        }


        .profile-container {
            background: linear-gradient(45deg, #d44fdc, #e38be3, #f0b7f5);
            border-radius: 15px;
            padding: 20px;
            width: 400px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
            transition: transform 0.5s ease, opacity 0.5s ease;
            transform: scale(0.8);
            opacity: 0;
        }

        .profile-container.active {
            opacity: 1;
            transform: scale(1);
        }

        .profile-arrow {
            font-size: 24px;
            cursor: pointer;
            color: #ffffff;
            position: relative;
            top: 40px;
            margin: 0 20px;
            transition: color 0.3s ease;
            display: block;
            z-index: 10;


        }

        .profile-arrow:hover {
            color: #e04e2e;
        }

        .profile-container {
            transition: transform 0.5s ease;
        }

        .profile-header {
            text-align: center;
            margin-bottom: 15px;
        }

        .profile-picture {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            margin-bottom: 10px;
            border: 2px solid #f093fb;
        }

        h1 {
            font-size: 1.5rem;
        }

        .profile-details {
            margin: 15px 0;
            text-align: left;
        }

        .edit-profile,
        .change-password,
        .subscription-block {
            background: #333;
            border-radius: 10px;
            padding: 15px;
            margin-top: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
        }

        button {
            padding: 0.75rem;
            background-color: #e04e2e;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        button:hover {
            background-color: #ff5c33;
            transform: translateY(-2px);
        }

        .subscription-block {
            background: linear-gradient(135deg, #6c63ff, #d44fdc);
        }

        #background-video {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -1;
        }

        .modify-profile-container {
            background: linear-gradient(45deg, #d44fdc, #e38be3, #f0b7f5);
            border-radius: 15px;
            padding: 20px;
            width: 400px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
            transition: opacity 0.5s ease;
            opacity: 0;
            margin-left: 20px;
            display: none;
        }

        .modify-profile-container.active {
            display: block;
            opacity: 1;
        }

        .preview-container {
            background: rgba(20, 20, 20, 0.8);
            /* Similar to the card background */
            border: 2px solid #00bfff;
            /* Same border color as card */
            border-radius: 15px;
            padding: 20px;
            width: 400px;
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.7), 0 0 40px rgba(0, 255, 255, 0.5);
            transition: opacity 0.5s ease;
            opacity: 0;
            margin-right: 20px;
            display: none;
        }

        .preview-container.active {
            display: block;
            opacity: 1;
        }

        .add-photo-container {
            position: relative;
            display: inline-block;
        }

        .add-photo {
            width: 100px;
            height: auto;
            border-radius: 10px;
            /* Same as the card img */
            margin-bottom: 10px;
            opacity: 0.5;
            /* Adjust this if needed */
            border: 2px solid #00bfff;
            /* To match the card */
        }

        .upload-label {
            position: absolute;
            top: 35px;
            /* Adjust position as needed */
            left: 0;
            /* Center it under the image */
            right: 0;
            text-align: center;
            font-size: 24px;
            color: #ffffff;
            text-shadow: 0 0 5px rgba(0, 0, 0, 0.5);
            background-color: rgba(0, 0, 0, 0.5);
            /* Semi-transparent background */
            border-radius: 5px;
            padding: 5px;
            transition: background-color 0.3s ease;
        }

        .upload-label:hover {
            background-color: rgba(0, 0, 0, 0.7);
        }


        .add-photo {
            width: 100px;
            height: auto;
            border-radius: 0;
            margin-bottom: 10px;
            opacity: 0.5;
        }
    </style>

    <body>
        <video autoplay muted loop id="background-video">
            <source src="/images/vecteezy_polygon-cyberpunk-sci-fi-background_1807028.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>
        <div class="main-content">
            <div class="profile-arrow left-arrow" title="Preview">&#9664;</div>
            <div class="preview" style="display: none;">
                <div class="preview-container active">
                    <h3>Preview</h3>
                    <div class="profile-header">
                        <h1>Profil</h1>
                        <div class="add-photo-container">
                            <img src="/uploads/<%= userDetails.e_photo %>" alt="Photo de Profil" class="add-photo">
                            <label for="file-upload" class="upload-label">+</label>
                            <input type="file" id="file-upload" style="display:none;" accept="image/*">
                        </div>
                    </div>
                    <div class="profile-details">
                        <p><strong>Nom:</strong> <span id="user-nom">
                                <%= userDetails.e_nom %>
                            </span></p>
                    </div>
                </div>
            </div>



            <div class="profile-container">
                <div class="profile-header">
                    <h1>Profil</h1>
                    <img src="/uploads/<%= userDetails.e_photo %>" alt="Photo de Profil" class="profile-picture">
                </div>
                <div class="profile-details">
                    <p><strong>Prénom:</strong> <span id="user-prenom">
                            <%= userDetails.e_prenom %>
                        </span></p>
                    <p><strong>Nom:</strong> <span id="user-nom">
                            <%= userDetails.e_nom %>
                        </span></p>
                    <p><strong>Email:</strong> <span id="user-email">
                            <%= userDetails.e_courriel %>
                        </span></p>
                    <p><strong>Votre abonnement actuel:</strong> <span>
                            <%= subscriptionName %>
                        </span></p>
                </div>

                <div class="profile-actions">
                    <form action="/event/delete-account" method="POST"
                        onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer votre compte? Cette action est irréversible.');">
                        <button type="submit">Supprimer le Compte</button>
                    </form>
                </div>

                <div class="subscription-block">
                    <h3>Votre abonnement actuel :</h3>
                    <p>
                        <%= subscriptionName %>
                    </p>
                    <p>Souhaitez-vous obtenir un autre abonnement ?</p>
                    <a href="/event/abonnement">
                        <button>Changer d'abonnement</button>
                    </a>
                </div>
            </div>
            <div class="modify-profile-container" style="display: none;">
                <h3>Modifier le Profil</h3>
                <div class="edit-profile">
                    <h3>Modifier les informations du profil</h3>
                    <button id="edit-profile-btn">Modifier le Profil</button>
                    <form id="update-profile-form" style="display: none;">
                        <input type="text" name="new_firstName" placeholder="Prénom" required>
                        <input type="text" name="new_lastName" placeholder="Nom" required>
                        <input type="email" name="new_email" placeholder="Email" required>
                        <button type="submit">Mettre à jour le profil</button>
                    </form>
                </div>

                <div class="change-password">
                    <h3>Changer le Mot de Passe</h3>
                    <button id="change-password-btn">Changer le Mot de Passe</button>
                    <form id="change-password-form" style="display: none;" method="POST"
                        action="/event/change-password">
                        <input type="password" name="old_password" placeholder="Ancien mot de passe" required>
                        <input type="password" name="new_password" placeholder="Nouveau mot de passe" required>
                        <input type="password" name="confirm_password" placeholder="Confirmer le nouveau mot de passe"
                            required>
                        <button type="submit">Changer le Mot de Passe</button>
                    </form>

                </div>
            </div>
            <div class="profile-arrow right-arrow" title="Modifier profil">&#9654;</div>


        </div>


        <script>
            window.addEventListener('load', function () {
                const profileContainer = document.querySelector('.profile-container');
                setTimeout(() => {
                    profileContainer.classList.add('active');
                }, 100);
            });

            const leftArrow = document.querySelector('.left-arrow');
            const rightArrow = document.querySelector('.right-arrow');
            const profileContainer = document.querySelector('.profile-container');
            const modifyProfileContainer = document.querySelector('.modify-profile-container');
            const preview = document.querySelector('.preview');

            let isProfileVisible = true;

            leftArrow.addEventListener('click', function () {
                console.log(isProfileVisible);
                if (!isProfileVisible) {
                    modifyProfileContainer.style.opacity = '0';
                    setTimeout(() => {
                        profileContainer.style.transition = 'transform 0.5s ease';
                        profileContainer.style.transform = 'translateX(0)';
                        modifyProfileContainer.classList.remove('active');
                        modifyProfileContainer.style.display = 'none';
                        rightArrow.style.display = 'block';
                        isProfileVisible = true;
                    }, 500);
                } else {
                    profileContainer.style.transition = 'transform 0.5s ease';
                    profileContainer.style.transform = 'translateX(100%)';
                    setTimeout(() => {
                        preview.classList.add('active');
                        preview.style.display = 'block';
                        preview.style.opacity = '1';
                        leftArrow.style.display = 'none';
                        rightArrow.style.display = 'block';
                        isProfileVisible = false;
                    }, 500);
                }
            });

            rightArrow.addEventListener('click', function () {
                console.log(isProfileVisible);
                if (!isProfileVisible) {
                    preview.style.opacity = '0';
                    setTimeout(() => {
                        profileContainer.style.transition = 'transform 0.5s ease';
                        profileContainer.style.transform = 'translateX(0)';
                        preview.classList.remove('active');
                        preview.style.display = 'none';
                        leftArrow.style.display = 'block';
                        isProfileVisible = true;
                    }, 500);
                } else {

                    profileContainer.style.transition = 'transform 0.5s ease';
                    profileContainer.style.transform = 'translateX(-100%)';
                    setTimeout(() => {
                        modifyProfileContainer.classList.add('active');
                        modifyProfileContainer.style.display = 'block';
                        modifyProfileContainer.style.opacity = '1';
                        leftArrow.style.display = 'block';
                        rightArrow.style.display = 'none';
                        isProfileVisible = false;
                    }, 500);
                }
            });






            document.getElementById("edit-profile-btn").addEventListener("click", function () {
                const form = document.getElementById("update-profile-form");
                form.style.display = form.style.display === "none" ? "block" : "none";
            });

            document.getElementById("change-password-btn").addEventListener("click", function () {
                const form = document.getElementById("change-password-form");
                form.style.display = form.style.display === "none" ? "block" : "none";
            });


            document.getElementById("change-password-form").addEventListener("submit", function (event) {
                event.preventDefault();

                const formData = new FormData(this);

                fetch("/event/change-password", {
                    method: "POST",
                    body: new URLSearchParams(formData),
                    headers: {
                        "Content-Type": "application/x-www-form-urlencoded"
                    }
                })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            alert(data.message); // Show success message
                            // Optionally, refresh the page or update the UI
                        } else {
                            alert(data.message); // Show error message from server
                        }
                    });
            });

            document.getElementById("update-profile-form").addEventListener("submit", function (event) {
                event.preventDefault(); // Prevent default form submission

                const formData = new FormData(this);

                fetch("/event/update-profile", {
                    method: "POST",
                    body: new URLSearchParams(formData), // Format data for POST
                    headers: {
                        "Content-Type": "application/x-www-form-urlencoded"
                    }
                })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            // Update displayed user info
                            document.querySelector("#user-prenom").textContent = formData.get("new_firstName");
                            document.querySelector("#user-nom").textContent = formData.get("new_lastName");

                            // Add delay to show message, then refresh
                            setTimeout(() => {
                                location.reload(); // Refresh page to show new data
                            }, 500);

                            alert(data.message);
                        } else {
                            alert(data.message); // Show error message from server
                        }
                    });
            });
        </script>
    </body>